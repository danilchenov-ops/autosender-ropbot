-- 021_qa_a4.sql — A3 разнесена на две метрики (решение Тимофея 25.08.2026):
--   A3 — доля речи менеджера (норма 40–60%),
--   A4 — длина самого долгого монолога (порог 90 секунд).
-- Раньше обе величины складывались в один балл, и по нему нельзя было понять,
-- в чём именно провал. Блок A теперь считается по четырём метрикам.
-- Идемпотентно.

ALTER TABLE call_qa ADD COLUMN IF NOT EXISTS a4 numeric;
